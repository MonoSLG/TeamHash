<div class="col-md-6 col-md-offset-3">
        <div class="row">
                <div class="col-md-6 col-md-offset-3">
                    <h2>
                        <b>aldair</b>
                    </h2>
                </div>
        </div>

        <form id="login-form" action="/gen_Homework/assign" method="post">
            <div class="col-md-2">
                <select name="subject" id="subject" class="form-control">
                    <% sub.forEach(function(item) { %>
                        <option value="">
                            <%= item.name %>
                        </option>
                        <% }); %>
                </select>
            </div>
    
            <div class="col-md-2">
                <select name="grade" id="grade" class="form-control">
                    <% for(var i=1; i < 12; i++) { %>
                        <option value="">
                            <%= i %>
                        </option>
                        <% } %>
                </select>
            </div>
            <div class="col-md-2">
                    <select name="grade" id="grade" class="form-control">
                        <% for(var i=1; i < 12; i++) { %>
                            <option value="">
                                <%= i %>
                            </option>
                            <% } %>
                    </select>
                </div>
            <div>
             <input class="col-md-2" type="datetime-local" >
            </div>
            <div>
                <button class="btn btn-primary col-md-offset-1" >+</button>
            </div>


            <br>
            <br>
            <br>
            <br>
            <br>
            <br>
            <br>
            <br>

            <div class="col-md-6 col-md-offset-3">
                    <button type="submit" class="btn btn-primary">Asignar</button>
            </div>
        </form>       

</div>
