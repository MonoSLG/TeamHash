<div class="col-md-12">
	<div class="row">
		<div class="col-md-6">
			<h2>Listar temas</h2>
		</div>
	</div>
	<table class="table table-striped table-bordered" id="table">
	<thead>
		<tr>
			<th>#</th>
			<th>Título</th>
			<th>Descripción</th>
			<th>Materia</th>
			<th>Acciones</th>
		</tr>
	</thead>
	<tbody>
		<% for(let i=0, len=data.length; i < len; i++) { %>
			<% let item = data[i] %>
			<tr>
				<td><%= i+1 %></td>
				<td><%= item.title %></td>
				<td><%= item.description %></td>
				<td><%= item.subject.name %></td>
				<td>
					<% let editURL= '/editTopic/' + item.id %>
					<a style="color:green" href="<%= editURL %>">Editar</a>
					&bull;
					<a style="color:red" href="#" onclick="remove('<%= item.id %>', '<%= item.name %>')">Eliminar</a>
				</td>
		<% }; %>
	</tbody>
</table>
</div>
<!--_______________________________________TO DELETE______________________________-->
<div class="modal fade" id="myModal" role="dialog">
	<div class="modal-dialog">
		<div class="modal-content">
			<div class="modal-header">
				 <button type="button" class="close" data-dismiss="modal">&times;</button>
				 <h4 class="modal-title">Eliminar tema</h4>
			</div>
			<div class="modal-body">
				<p>¿Está seguro de querer eliminar éste tema?</p>
				<p id="textModel"></p>
			</div>
			<div class="modal-footer">
				<a id="linkModalRemove" class="btn btn-danger" href="#">Elminar</a>
				<button type="button" class="btn btn-default" data-dismiss="modal" >Cerrar</button>
			</div>
		</div>
	</div>
</div>

<script>
	function remove(id, name){
		$('#linkModalRemove').attr("href", "/gen_course/delete/"+id);
		$('#textModel').html("<strong>Name:</strong> " + name);
		$('#myModal').modal('show')
	}
</script>
<!--______________________________________________________________________________-->